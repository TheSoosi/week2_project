export default class User {
  constructor(username, email, address, admin, imageSrc) {
    this.username = username;
    this.email = email;
    this.address = address;
    this.admin = admin;
    this.imageSrc = imageSrc;
  }
}
